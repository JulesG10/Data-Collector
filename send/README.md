# Send

> Build send to exe
```bash
npm install && npm run build
```
> Send usage
```bash
send <url> <method> [-file=<filename>]
```
> Example
```bash
send https://www.google.com/ GET -file=google
```

